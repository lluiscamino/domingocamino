{
  "name": "domingo-camino-portfolio",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-html.js && npx esbuild dist/js/main.js --minify --outfile=dist/js/main.js --allow-overwrite && npx esbuild dist/css/style.css --minify --outfile=dist/css/style.css --allow-overwrite",
    "dev": "node scripts/build-html.js && npx serve dist"
  }
}
